node_modules/
dist/
*.log
.env
.env.*
!.env.example
.DS_Store
coverage/
vitest.config.ts.timestamp-*.mjs

# CLI per-user state — region/profile/endpoint defaults and a pointer to
# the user's Dash0 token (Secrets Manager ARN or local file path). Never
# commit; users generate it via `dash0-lambda` interactive Install or the
# Config screen.
.dash0-lambda.json
.dash0-lambda.token
